# SecurityAlertScraper
This python bot scrapes data from /www.oracle.com/security-alerts for a given year and month. The scraped data is then stored in CSV files. Every Security alert get it's own CSV file.
